# Copy this file to `.env` and set values for local development.
# Do NOT commit your real .env to source control.
SECRET_KEY=change-me-to-a-random-secret
# If you want to use a different DB path or remote DB, set DATABASE_URL
# DATABASE_URL=sqlite:///instance/parking.db
